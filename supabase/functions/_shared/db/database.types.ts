// Re-export of the auto-generated Supabase Database type.
// Source of truth = src/integrations/supabase/types.ts (regenerated via `supabase gen types typescript`).
// Shared between Vite (via @shared alias) and Deno (relative path resolution).
// NOTE: if types.ts is moved, update this path.
export type { Database } from "../../../../src/integrations/supabase/types.ts";
